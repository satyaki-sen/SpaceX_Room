<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.satyaki.taskandroid.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <action
            android:id="@+id/action_homeFragment_to_wikiFragment"
            app:destination="@id/wikiFragment" />
    </fragment>
    <fragment
        android:id="@+id/wikiFragment"
        android:name="com.satyaki.taskandroid.WikiFragment"
        android:label="Wikipedia"
        tools:layout="@layout/fragment_wiki">
        <action
            android:id="@+id/action_wikiFragment_to_homeFragment"
            app:destination="@id/homeFragment" />
    </fragment>
</navigation>